# Machine-Learning-Arbitrary-Value-Imputation
This repository demonstrates Arbitrary Value Imputation, a feature engineering technique for handling missing data in ML. Using the Titanic dataset, the notebook explores imputing nulls with extreme values like -1 or 99. It evaluates the impact on feature correlation and variance, providing a guide for pre-processing non-random missingness.
